[package]
name = "minerider-codegen"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "minecraft-data driven packet definition generator for minerider-protocol"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
